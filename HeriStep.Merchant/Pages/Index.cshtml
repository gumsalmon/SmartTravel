
@page
@model HeriStep.Merchant.Pages.IndexModel
@{
    ViewData["Title"] = "Chọn Sạp Quản Lý";
}

<div class="container py-5">
    <div class="text-center mb-5">
        <h2 class="fw-bold text-dark">Xin chào, <span class="text-primary">@User.Identity?.Name</span>! 👋</h2>
        <p class="text-muted fs-5">Hôm nay bạn muốn quản lý sạp hàng nào?</p>
    </div>

    <div class="row justify-content-center g-4">
        @if (Model.MyStalls.Any())
        {
            foreach (var stall in Model.MyStalls)
            {
                <div class="col-md-4 col-sm-6">
                    <a href="/StallDashboard?stallId=@stall.Id" class="text-decoration-none">
                        <div class="card h-100 border-0 shadow-sm rounded-4 hover-zoom" style="transition: transform 0.2s;">
                            <img src="@(string.IsNullOrEmpty(stall.ImageUrl) ? "/images/default-stall.jpg" : stall.ImageUrl)"
                                 class="card-img-top rounded-top-4" alt="@stall.Name" style="height: 200px; object-fit: cover;">

                            <div class="card-body text-center p-4">
                                <h5 class="card-title fw-bold text-dark mb-2">@stall.Name</h5>

                                @if (stall.IsOpen)
                                {
                                    <span class="badge bg-success-subtle text-success border border-success-subtle px-3 py-2 rounded-pill">
                                        <i class="bi bi-door-open-fill me-1"></i> Đang mở cửa
                                    </span>
                                }
                                else
                                {
                                    <span class="badge bg-danger-subtle text-danger border border-danger-subtle px-3 py-2 rounded-pill">
                                        <i class="bi bi-door-closed-fill me-1"></i> Đã đóng cửa
                                    </span>
                                }
                            </div>
                        </div>
                    </a>
                </div>
            }
        }
        else
        {
            <div class="col-12 text-center py-5">
                <div class="alert alert-warning d-inline-block px-5 shadow-sm rounded-4">
                    <i class="bi bi-exclamation-circle-fill fs-1 d-block mb-3 text-warning"></i>
                    <h5 class="fw-bold">Bạn chưa có sạp hàng nào!</h5>
                    <p class="mb-0">Vui lòng liên hệ Admin để được phân công sạp hàng.</p>
                </div>
            </div>
        }
    </div>
</div>

<style>
    .hover-zoom:hover {
        transform: translateY(-5px);
        box-shadow: 0 .5rem 1rem rgba(0,0,0,.15) !important;
        cursor: pointer;
    }
</style>